/* Status langkah pada navigasi langkah formulir (rekap, arsip gabungan, honorarium).
   Rel biru laut yang dulu memakai berkas ini sudah diganti navigasi langkah di atas. */

export type RailStatus = "done" | "required" | "open" | "optional" | "error";

# Node.js 24+. Copy to .env.local before the first run.
DEMO_ENABLED=false
ADMIN_EMAIL=operator@esdm.local
# Set a unique password of at least 12 characters before starting.
ADMIN_PASSWORD=
# Optional: absolute persistent storage directory.
# DATA_DIR=/absolute/path/rek-app-data
